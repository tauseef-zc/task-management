components:
  responses:
    LoginSuccess:
      description: Successful login
      content:
        application/json:
          schema:
            $ref: "./schemas.yml#/components/schemas/LoginResponse"

    Unauthorized:
      description: Unauthorized access
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
